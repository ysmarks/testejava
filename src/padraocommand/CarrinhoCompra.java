package padraocommand;

import java.util.List;
import java.util.Map;
import java.util.Set;

import padraodependencyinjection.CienteUsuario;

public class CarrinhoCompra {

	/*
	 * private Map<String, ComandoCarrinho> carrinho; private List<Produto>
	 * produtos; private Usuario usuario;
	 * 
	 * public Object executaComando(String nomeComando) throws
	 * ComandoNaoEncontradoException {
	 * 
	 * ComandoCarrinho c = carrinho.get(nomeComando); if (c == null) { throw new
	 * ComandoNaoEncontradoException(mensagem); }if(c instanceof CienteDoProduto) {
	 * (CienteDoProduto(c)).setListaProdutos(produtos); }if(c instanceof
	 * CienteUsuario) { (CienteUsuario(c)).setUsuario(usuario); } return
	 * c.executar();
	 * 
	 * }
	 * 
	 * public Set<String> getComandosDisponiveis() { return carrinho.keySet(); }
	 */
}
